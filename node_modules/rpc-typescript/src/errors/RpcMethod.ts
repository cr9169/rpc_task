
export class RpcMethod extends Error {

    constructor(
        methodName?: string,
        message?: string
    ) {
        super(message);

        this.name = typeof methodName === 'undefined' ? 'InvalidRpcMethod' : `InvalidRpcMethod(${methodName})`;
    }
}